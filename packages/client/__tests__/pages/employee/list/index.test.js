// const { render, screen } = require("@testing-library/react");
// const { default: Employees } = require("pages/employee/list")

describe('employee list', () => {
    test('renders a list', () => {
        // render(<Employees />);

        // expect(screen.getByTestId('btnToggle')).toBeInTheDocument();
        expect(1).toBe(1);
    })
})
